[package]
edition = "2021"
name = "hello-world"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bridge = {path = "../../crates/bridge", package = "wasm-lambda-bridge", features = ["message", "api"]}
